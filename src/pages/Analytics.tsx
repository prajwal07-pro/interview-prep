const Analytics = () => (
  <div className="flex min-h-screen items-center justify-center bg-background">
    <p className="text-muted-foreground">Analytics dashboard — coming in Phase 5.</p>
  </div>
);

export default Analytics;
