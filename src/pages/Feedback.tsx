const Feedback = () => (
  <div className="flex min-h-screen items-center justify-center bg-background">
    <p className="text-muted-foreground">Feedback screen — coming in Phase 4.</p>
  </div>
);

export default Feedback;
